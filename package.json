{
  "dependencies": {
    "axios": "^1.13.2",
    "date-fns": "^4.1.0",
    "notistack": "^3.0.2"
  }
}

{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host",
    "serve": "serve dist -s -l"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}